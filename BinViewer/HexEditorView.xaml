<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BinViewer.HexEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BinViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">

        <!-- Фиксированный заголовок таблицы -->
        <Grid Grid.Row="0" ColumnDefinitions="90, 410, 160" Height="28" Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
            <!-- Колонка адреса -->
            <TextBlock Grid.Column="0" Text="Offset" FontFamily="Consolas" FontSize="14" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>

            <!-- Позиции всех 16 байтов (00-0F) -->
            <TextBlock Grid.Column="1" Text="00 01 02 03 04 05 06 07 08 09 0A 0B 0C 0D 0E 0F" FontFamily="Consolas" FontSize="14" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center"/>

            <!-- Текстовая колонка -->
            <TextBlock Grid.Column="2" Text="ASCII" FontFamily="Consolas" FontSize="14" FontWeight="Bold" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" VerticalAlignment="Center" Margin="5,0,0,0"/>
        </Grid>

        <!-- Прокручиваемый список с байтами -->
        <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="1" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Auto">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Rows, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:HexRow">
                        <!-- Разметка колонок строк теперь строго совпадает с расширенным заголовком -->
                        <Grid ColumnDefinitions="90, 410, 160" Height="24" AutomationProperties.Name="HexRow">
                            <!-- Адрес -->
                            <TextBlock Grid.Column="0" Text="{x:Bind AddressHex}" FontFamily="Consolas" FontSize="14" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                            <!-- HEX байты -->
                            <TextBlock Grid.Column="1" Text="{x:Bind HexString}" FontFamily="Consolas" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <!-- ASCII представление -->
                            <TextBlock Grid.Column="2" Text="{x:Bind AsciiString}" FontFamily="Consolas" FontSize="14" Foreground="DarkGreen" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
